# 🎨 Design System: Fintech Neumorphism

> **Style**: Bold Fintech + Neumorphism + Dark Mode
> 
> **Vibe**: Modern, Trust-driven, Professional, Elegant

---

## 🖼️ Design Philosophy

```
┌─────────────────────────────────────────────────────────────────┐
│                    DESIGN PRINCIPLES                             │
├─────────────────────────────────────────────────────────────────┤
│  🌑 DARK MODE         - Deep, rich backgrounds                  │
│  💎 NEUMORPHISM       - Soft shadows, subtle depth              │
│  ✨ BOLD TYPOGRAPHY   - Strong hierarchy, clear CTAs            │
│  📊 DATA-FIRST        - Powerful visuals, trust signals         │
│  🎯 INTUITIVE         - Smooth flow, clear navigation           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎨 Color Palette

### Primary Colors
```css
:root {
  /* Background Layers (Dark Mode) */
  --bg-deepest: #0a0a0f;        /* Darkest background */
  --bg-deep: #0f0f17;           /* Main background */
  --bg-card: #14141f;           /* Card background */
  --bg-elevated: #1a1a28;       /* Elevated surfaces */
  --bg-hover: #22223a;          /* Hover states */
  
  /* Neumorphism Shadows */
  --shadow-dark: rgba(0, 0, 0, 0.5);
  --shadow-light: rgba(255, 255, 255, 0.03);
  
  /* Accent Colors (Fintech Gradients) */
  --accent-primary: #6366f1;     /* Indigo */
  --accent-secondary: #8b5cf6;   /* Purple */
  --accent-tertiary: #a855f7;    /* Violet */
  
  /* Status Colors */
  --success: #10b981;            /* Emerald */
  --success-glow: rgba(16, 185, 129, 0.2);
  
  --warning: #f59e0b;            /* Amber */
  --warning-glow: rgba(245, 158, 11, 0.2);
  
  --danger: #ef4444;             /* Red */
  --danger-glow: rgba(239, 68, 68, 0.2);
  
  --info: #3b82f6;               /* Blue */
  --info-glow: rgba(59, 130, 246, 0.2);
  
  /* Text Colors */
  --text-primary: #f8fafc;       /* White */
  --text-secondary: #94a3b8;     /* Slate 400 */
  --text-muted: #64748b;         /* Slate 500 */
  --text-subtle: #475569;        /* Slate 600 */
}
```

### Gradient Presets
```css
/* Hero/CTA Gradient */
.gradient-primary {
  background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 50%, #a855f7 100%);
}

/* Card Glow Effect */
.gradient-card {
  background: linear-gradient(145deg, #1a1a28 0%, #14141f 100%);
}

/* Success Indicator */
.gradient-success {
  background: linear-gradient(135deg, #10b981 0%, #059669 100%);
}

/* Danger/Alert */
.gradient-danger {
  background: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
}
```

---

## 🌟 Neumorphism Components

### Base Neumorphism Mixin
```css
/* Raised Card (Default) */
.neu-raised {
  background: var(--bg-card);
  border-radius: 20px;
  box-shadow: 
    8px 8px 16px var(--shadow-dark),
    -4px -4px 12px var(--shadow-light),
    inset 0 0 0 1px rgba(255, 255, 255, 0.03);
}

/* Pressed/Inset Card */
.neu-inset {
  background: var(--bg-deepest);
  border-radius: 16px;
  box-shadow: 
    inset 6px 6px 12px var(--shadow-dark),
    inset -3px -3px 8px var(--shadow-light);
}

/* Floating Card (Elevated) */
.neu-floating {
  background: var(--bg-elevated);
  border-radius: 24px;
  box-shadow: 
    12px 12px 24px var(--shadow-dark),
    -6px -6px 16px var(--shadow-light),
    0 0 40px rgba(99, 102, 241, 0.1);
}

/* Button */
.neu-button {
  background: linear-gradient(145deg, #1a1a28, #14141f);
  border-radius: 12px;
  box-shadow: 
    4px 4px 8px var(--shadow-dark),
    -2px -2px 6px var(--shadow-light);
  transition: all 0.2s ease;
}

.neu-button:hover {
  box-shadow: 
    6px 6px 12px var(--shadow-dark),
    -3px -3px 8px var(--shadow-light),
    0 0 20px rgba(99, 102, 241, 0.2);
}

.neu-button:active {
  box-shadow: 
    inset 4px 4px 8px var(--shadow-dark),
    inset -2px -2px 6px var(--shadow-light);
}
```

---

## 📐 Layout System

### Page Structure
```
┌──────────────────────────────────────────────────────────────────────────┐
│  SIDEBAR (280px)  │              MAIN CONTENT AREA                       │
│                   │                                                       │
│  ┌─────────────┐  │  ┌─────────────────────────────────────────────────┐ │
│  │   LOGO      │  │  │              PAGE HEADER                        │ │
│  │   + Brand   │  │  │  Title + Breadcrumb + Actions                   │ │
│  └─────────────┘  │  └─────────────────────────────────────────────────┘ │
│                   │                                                       │
│  ┌─────────────┐  │  ┌──────────────────────────────────────────────────┐│
│  │ Navigation  │  │  │                                                  ││
│  │ ─────────── │  │  │              CONTENT AREA                        ││
│  │ □ Dashboard │  │  │                                                  ││
│  │ □ Forecast  │  │  │  ┌──────────┐ ┌──────────┐ ┌──────────┐         ││
│  │ □ Health    │  │  │  │  Card 1  │ │  Card 2  │ │  Card 3  │         ││
│  │ □ Alerts    │  │  │  └──────────┘ └──────────┘ └──────────┘         ││
│  │ □ Reorder   │  │  │                                                  ││
│  │ □ Slow-Move │  │  │  ┌─────────────────────────────────────────────┐ ││
│  │ □ MBA       │  │  │  │              DATA TABLE / CHART             │ ││
│  │ □ RFM       │  │  │  │                                             │ ││
│  │ □ Settings  │  │  │  └─────────────────────────────────────────────┘ ││
│  └─────────────┘  │  │                                                  ││
│                   │  └──────────────────────────────────────────────────┘│
│  ┌─────────────┐  │                                                       │
│  │ Quick Stats │  │                                                       │
│  │ + Activity  │  │                                                       │
│  └─────────────┘  │                                                       │
└───────────────────┴───────────────────────────────────────────────────────┘
```

### Grid System
```css
/* 12-column grid */
.grid-container {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 24px;
  padding: 24px;
}

/* Metric Cards Row: 4 columns */
.metric-row {
  grid-template-columns: repeat(4, 1fr);
}

/* Chart + Table: 8-4 split */
.chart-area { grid-column: span 8; }
.side-panel { grid-column: span 4; }

/* Full width sections */
.full-width { grid-column: span 12; }
```

---

## 🧩 Component Designs

### 1. Metric Card (Neumorphic)
```
┌─────────────────────────────────────┐
│  ┌──────┐                           │
│  │ 💀   │  Critical Alerts          │
│  └──────┘                           │
│                                     │
│  ████████  247                      │
│                                     │
│  ↑ 12% from last week              │
│  ─────────────────────────────────  │
│  Mini sparkline chart               │
└─────────────────────────────────────┘
     ▓▓▓▓▓▓▓▓▓▓▓ (glow effect)
```

```css
.metric-card {
  background: var(--bg-card);
  border-radius: 20px;
  padding: 24px;
  position: relative;
  overflow: hidden;
  
  /* Neumorphism */
  box-shadow: 
    8px 8px 16px var(--shadow-dark),
    -4px -4px 12px var(--shadow-light);
  
  /* Subtle gradient border */
  border: 1px solid rgba(255, 255, 255, 0.05);
}

.metric-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--gradient-primary);
  border-radius: 20px 20px 0 0;
}

.metric-card.status-critical::before {
  background: var(--gradient-danger);
  box-shadow: 0 0 20px var(--danger-glow);
}
```

### 2. Sidebar Navigation
```
┌───────────────────────────────┐
│                               │
│   ◉ INVENTORY INTELLIGENCE    │
│      PT Wahana Piranti        │
│                               │
│   ───────────────────────     │
│                               │
│   ┌─────────────────────────┐ │
│   │ ● Dashboard Overview    │◄── Active (glowing)
│   └─────────────────────────┘ │
│   │   Demand Forecasting    │ │
│   │   Inventory Health      │ │
│   │   Stockout Alerts    ●  │◄── Has notifications
│   │   Reorder Optimization  │ │
│   │   Slow-Moving Analysis  │ │
│   │   Market Basket (MBA)   │ │
│   │   RFM Analysis          │ │
│   │   Settings              │ │
│                               │
│   ───────────────────────     │
│                               │
│   ┌─────────────────────────┐ │
│   │  QUICK STATS            │ │
│   │  ─────────────          │ │
│   │  🔴 25 Active Alerts    │ │
│   │  📦 2,136 Products      │ │
│   │  ⏰ 12:48:29            │ │
│   └─────────────────────────┘ │
│                               │
│   ┌─────────────────────────┐ │
│   │  👤 admin1 • Logout     │ │
│   └─────────────────────────┘ │
└───────────────────────────────┘
```

### 3. Data Table (Fintech Style)
```
┌─────────────────────────────────────────────────────────────────────────┐
│  📋 Inventory Products                               🔍 Search...  ⚙️  │
├─────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │ CODE      │ NAME           │ STOCK    │ RISK     │ DAYS    │ ACT  ││
│  ├───────────┼────────────────┼──────────┼──────────┼─────────┼──────┤│
│  │ PRD-001   │ Widget Alpha   │ 1,234    │ 🟢 LOW   │ 45      │ •••  ││
│  │ PRD-002   │ Widget Beta    │ 23       │ 🔴 CRIT  │ 3       │ •••  ││
│  │ PRD-003   │ Widget Gamma   │ 456      │ 🟡 MED   │ 18      │ •••  ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                         │
│  ← 1  2  3  4  5 ... 43 →                      Showing 1-50 of 2,136   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4. Chart Cards
```
┌─────────────────────────────────────────────────────────────────┐
│  📊 Stock Distribution by Risk Level              ⋮  📥  🔄    │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│              ┌─────────────────────────────────┐               │
│              │                                 │               │
│              │     ██████████                  │               │
│              │   ██          ██                │               │
│              │  █   DONUT     █                │               │
│              │  █   CHART     █                │               │
│              │   ██          ██                │               │
│              │     ██████████                  │               │
│              │         2,136                   │               │
│              │        Products                 │               │
│              └─────────────────────────────────┘               │
│                                                                 │
│  🔴 Critical: 45 (2.1%)    🟠 High: 123 (5.8%)                 │
│  🟡 Medium: 456 (21.3%)    🟢 Low: 1,512 (70.8%)               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📱 Page-Specific Layouts

### Dashboard Overview
```
┌──────────────────────────────────────────────────────────────────────┐
│  HEADER: Dashboard Overview                           🔔  👤  ⚙️    │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │ CRITICAL │ │   HIGH   │ │  MEDIUM  │ │   LOW    │  ← Risk Cards │
│  │    45    │ │   123    │ │   456    │ │  1,512   │               │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘               │
│                                                                       │
│  ┌─────────────────────────────┐ ┌─────────────────────────────────┐ │
│  │     STOCK DISTRIBUTION      │ │      TREND ANALYSIS            │ │
│  │        (Pie Chart)          │ │       (Line Chart)             │ │
│  └─────────────────────────────┘ └─────────────────────────────────┘ │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐ │
│  │                    TOP ALERTS TABLE                             │ │
│  │  Urgent items requiring immediate attention                     │ │
│  └─────────────────────────────────────────────────────────────────┘ │
│                                                                       │
│  ┌────────────────────┐ ┌────────────────────┐ ┌──────────────────┐ │
│  │  Quick Actions     │ │  Recent Activity   │ │  System Health   │ │
│  │  • Export Report   │ │  • Login 12:30     │ │  ● All OK        │ │
│  │  • Run Pipeline    │ │  • Refresh 12:28   │ │  CPU: 23%        │ │
│  └────────────────────┘ └────────────────────┘ └──────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

### Stockout Alerts
```
┌──────────────────────────────────────────────────────────────────────┐
│  HEADER: ⚠️ Stockout Alerts                     Filter ▼  Export 📥 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  🔴 CRITICAL ZONE                                     12 items │  │
│  │  ─────────────────────────────────────────────────────────────  │  │
│  │                                                                 │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │  │
│  │  │ PRD-001 │ │ PRD-005 │ │ PRD-012 │ │ PRD-023 │   ...        │  │
│  │  │ 2 days  │ │ 3 days  │ │ 5 days  │ │ 6 days  │              │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  🟠 HIGH RISK                                         45 items │  │
│  │  ... (similar card layout)                                     │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │  FULL ALERTS TABLE (expandable)                                │  │
│  └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔤 Typography

```css
/* Font Stack */
--font-display: 'Inter', 'SF Pro Display', system-ui, sans-serif;
--font-mono: 'JetBrains Mono', 'Fira Code', monospace;

/* Sizes */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
--text-4xl: 2.25rem;   /* 36px */
--text-5xl: 3rem;      /* 48px */

/* Headings */
h1 { font-size: var(--text-4xl); font-weight: 700; letter-spacing: -0.02em; }
h2 { font-size: var(--text-2xl); font-weight: 600; }
h3 { font-size: var(--text-xl); font-weight: 600; }

/* Metric Numbers */
.metric-number {
  font-size: var(--text-5xl);
  font-weight: 800;
  letter-spacing: -0.03em;
  background: linear-gradient(135deg, #fff 0%, #94a3b8 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
```

---

## ✨ Animations & Micro-interactions

```css
/* Smooth transitions */
* {
  transition: background 0.2s ease, 
              box-shadow 0.2s ease, 
              transform 0.2s ease;
}

/* Card hover lift */
.card:hover {
  transform: translateY(-4px);
  box-shadow: 
    12px 12px 24px var(--shadow-dark),
    -6px -6px 16px var(--shadow-light),
    0 0 30px rgba(99, 102, 241, 0.15);
}

/* Button press effect */
.btn:active {
  transform: scale(0.98);
}

/* Glow pulse for critical items */
@keyframes pulse-glow {
  0%, 100% { box-shadow: 0 0 20px var(--danger-glow); }
  50% { box-shadow: 0 0 40px var(--danger-glow); }
}

.critical-item {
  animation: pulse-glow 2s infinite;
}

/* Loading skeleton */
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

.skeleton {
  background: linear-gradient(90deg, 
    var(--bg-card) 25%, 
    var(--bg-elevated) 50%, 
    var(--bg-card) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}
```

---

## 📋 Implementation Checklist

When building each page, ensure:

- [ ] Dark background with proper depth layers
- [ ] Neumorphic shadows on all cards
- [ ] Gradient accents on primary CTAs
- [ ] Status colors with glow effects
- [ ] Smooth hover/press animations
- [ ] Bold, clear typography hierarchy
- [ ] Trust signals (data counts, timestamps)
- [ ] Responsive grid layout

---

> **Next Step**: Lanjut ke implementasi dengan request: "Mulai Step 1: Setup backend Flask"
